<%= form_for @image, :html => { :multipart => true }  do |f| %>
  <div class="fields">
    <%= f.label :image, "File" %>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit%>
  </div>
<% end %>
